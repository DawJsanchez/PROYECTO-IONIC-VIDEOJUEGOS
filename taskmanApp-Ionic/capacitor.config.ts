import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'taskmanApp-Ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
